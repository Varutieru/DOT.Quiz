"use client";

import Header from "../ui/Header";

export default function Hero() {
    return (
        <section className="w-full h-screen px-[5vw] py-[5vh] bg-[radial-gradient(circle_at_center,#191629,#141121)]">
            <Header />
        </section>
    )
}